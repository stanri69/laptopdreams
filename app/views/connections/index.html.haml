%h1 Listing connections

%table
  %thead
    %tr
      %th Network adapters
      %th Ports
      %th Wireless communication
      %th
      %th
      %th

  %tbody
    - @connections.each do |connection|
      %tr
        %td= connection.network_adapters
        %td= connection.ports
        %td= connection.wireless_communication
        %td= link_to 'Show', connection
        %td= link_to 'Edit', edit_connection_path(connection)
        %td= link_to 'Destroy', connection, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Connection', new_connection_path
